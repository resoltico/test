rules:
  preformatted:
    filter: "pre"
    replacement: "\n\n```\n{content}\n```\n\n"
    
  preformattedWithCode:
    filter: "pre > code"
    replacement: "{content}"
    
  codeWithLanguage:
    filter: "pre > code[class*='language-']"
    replacement: "{content}"
